node_modules
dist
# Keep environment variables out of version control
.env

/src/generated/prisma
*.tsbuildinfo

# รูปใบเสร็จ/ป้าย/เล่มที่เก็บในเครื่อง (LocalReceiptStorage): uploads/receipts, uploads/plates, uploads/books
/uploads

# ผลทดสอบการอ่านใบเสร็จ (มีทะเบียน/เลขตัวถังของลูกค้าจริง)
/scripts/out
