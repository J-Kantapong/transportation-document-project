# Transportation Document Project

Private workspace for a Thai transport-registration application.

## Development surfaces

- `frontend/` — Next.js frontend
- `backend/` — NestJS backend
- `prototype/sites-reference/` — working Codex/Sites prototype and the current UI/data-contract reference

Claude Code should open the repository root so it reads the root `CLAUDE.md`. Codex and Claude can then work from the same GitHub repository while keeping the existing frontend/backend structure.

The prototype does not include production customer records or credentials. Its `.openai/hosting.json` is intentionally excluded from this repository copy.

